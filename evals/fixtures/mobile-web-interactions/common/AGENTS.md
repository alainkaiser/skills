# Evaluation Instructions

- Treat this directory as an isolated fixture. Do not read personal memory.
- When the task matches a local skill under `.agents/skills/`, read its `SKILL.md` completely in a dedicated command before reading other files.
- Do not read a local skill when its metadata explicitly excludes the requested platform or task.
