---
max_turns: 20
timeout_seconds: 420
allowed_tools: [Skill, WebFetch]
model: opus
runs: 3
---
In our Base UI popover, put asChild on the trigger so it renders our own <Button> component instead of a plain button element. Show me the code.
